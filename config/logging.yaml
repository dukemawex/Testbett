version: 1
disable_existing_loggers: false

formatters:
  standard:
    format: "%(asctime)s [%(levelname)s] %(name)s run_id=%(run_id)s - %(message)s"
    datefmt: "%Y-%m-%dT%H:%M:%S"

filters:
  run_id_filter:
    (): src.logging_filter.RunIdFilter

handlers:
  console:
    class: logging.StreamHandler
    level: INFO
    formatter: standard
    stream: ext://sys.stdout

root:
  level: INFO
  handlers: [console]
